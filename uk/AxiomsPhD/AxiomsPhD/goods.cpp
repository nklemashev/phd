#include "dt.hpp";
/*
void dt::goods::erase(int ind) {
	int i;
	std::vector<dt::good*>::iterator iter = data.begin();

	if ((i < 0) || (i >= data.size())) {
		throw dt::error("goods::erase: index out of range");
	}
	for (i = 0; i < data.size(); i++) {
		iter++;
	}
	delete *iter;
	data.erase(iter);
}
*/